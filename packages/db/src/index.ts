/**
 * Re-exports the generated Prisma client.
 *
 * Note: the actual `PrismaClient` lives at `@prisma/client` and is generated
 * from `prisma/schema.prisma`. Run `pnpm db:generate` before importing.
 * After generation, the model types below become available via the wildcard
 * re-export; they are listed here only as documentation.
 */
export { PrismaClient, Prisma } from "@prisma/client";
export * from "@prisma/client";